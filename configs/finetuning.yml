

epochs: 30
batch_size: 512

lr: 0.00001
weight_decay: 1e-5

num_workers: 10

emb_size: 256
heads: 8
depth: 4
criterion_name: "focal"  # oppure: "bce", "focal"
focal_alpha: 0.8
focal_gamma: 0.7
threshold: 0.5

n_channels: 18
n_classes: 1

early_stopping_patience: 20
save_every: 10
resume: False


dataset_path: "../../Datasets/Bipolar/chb_mit/bipolar_data"
pretrained_ckpt: ./logs/pretrain/model_epoch_25.pt

save_dir: ./checkpoints_finetuning
log_dir: ./logs/finetuning_from_scratch

finetune_mode: "from_scratch"  # oppure: "from_scratch", "full_finetune", "frozen_encoder" 
